# What to do when a task is completed
- Run relevant tests (`pytest -v` or targeted test files in `tests/`).
- If behavior changes, update README/CONTRIBUTING as needed.
- Verify `.env` or docker env examples if new settings are introduced.
